/** Private bucket created by migration `banner_designs_and_versions`. */
export const BANNER_ASSETS_BUCKET = "banner-assets";

/** Signed URLs for <img src>; refresh on history load if expired. */
export const BANNER_ASSET_SIGNED_URL_TTL_SEC = 60 * 60 * 24 * 7;
