OPENAI_API_KEY=
# Required on Vercel: create a Blob store and add the read/write token (Dashboard → Storage → Blob).
BLOB_READ_WRITE_TOKEN=

# Supabase (server: storage + Postgres via service role; browser: Auth session via anon key)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
